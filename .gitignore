# Gradle build directories (anchored to root and app module — never match source packages)
.gradle/
/build/
app/build/
*.class

# Local configuration — contains SDK path, never commit
local.properties

# Log / OS files
*.log
.DS_Store
Thumbs.db

# Android Studio / IntelliJ
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml

# Build outputs
*.apk
*.aab
output-metadata.json

# Keystore files — never commit signing keys
*.jks
*.keystore

# Google services
google-services.json

# Profiling
*.hprof

# The actual proot binary is a binary blob; commit only the README placeholder.
# Obtain via: ./scripts/get_proot.sh
app/src/main/jniLibs/arm64-v8a/libproot.so
